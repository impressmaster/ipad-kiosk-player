# Reproductor para iPad

Reproductor web a pantalla completa para publicar mediante GitHub Pages y abrir como Web Clip desde Miradore.

La programación se configura en `playlist.json`. Cada vez que cambie, incrementa el campo `version`.
